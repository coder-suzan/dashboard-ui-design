function logintogglePassword() {
    var togglePass = document.getElementById("loginPassword");
    if (togglePass.type === "password") {
      togglePass.type = "text";
    } else {
      togglePass.type = "password";
    }
  }

  function newtogglePassword() {
    var newTogglePass = document.getElementById("newPassword");
    if (newTogglePass.type === "password") {
      newTogglePass.type = "text";
    } else {
      newTogglePass.type = "password";
    }
  }

  function retogglePassword() {
    var reTogglePass = document.getElementById("rePassword");
    if (reTogglePass.type === "password") {
      reTogglePass.type = "text";
    } else {
      reTogglePass.type = "password";
    }
  }

  function addToggleFunction() {
    var addPassword = document.getElementById("addPassword");
    if (addPassword.type === "password") {
      addPassword.type = "text";
    } else {
      addPassword.type = "password";
    }
  }

  function changeStudentToggleFunction() {
    var changeStudentPassword = document.getElementById("changeStudentPassword");
    if (changeStudentPassword.type === "password") {
      changeStudentPassword.type = "text";
    } else {
      changeStudentPassword.type = "password";
    }
  }



  function addStudentPasswordToggle() {
    var addStudentPassword = document.getElementById("addStudentPassword");
    if (addStudentPassword.type === "password") {
      addStudentPassword.type = "text";
    } else {
      addStudentPassword.type = "password";
    }
  }